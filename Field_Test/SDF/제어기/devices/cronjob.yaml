apiVersion: batch/v1
kind: CronJob
metadata:
  name: devices-check
  labels:
    framework: sdlmicro
spec:
  schedule: "* * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: Never
          imagePullSecrets:
            - name: regcred
          containers:
          - name: devices-check
            image: registry.suredatalab.kr/beymons/devices:latest
            args:
              - check
            envFrom: []
