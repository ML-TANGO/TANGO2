# apiVersion: v1
# kind: PersistentVolume
# metadata:
#   name: jfb-{{ .Values.global.developer }}-built-in-model-pv
# spec:
#   capacity:
#     storage: 1000Gi
#   accessModes:
#     - ReadWriteMany
#   claimRef:
#     namespace: {{ .Values.global.developer }}
#     name: jfb-{{ .Values.global.developer }}-built-in-model-pvc
#   persistentVolumeReclaimPolicy: Retain
#   hostPath:
#     path: {{ include "dir.bin" . }}/built_in_models

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jfb-{{ .Values.global.developer }}-built-in-model-pvc
  namespace: {{ .Values.global.developer }}
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1000Gi
  {{- if .Values.volume.storageClass.enabled }}
  storageClassName: {{ .Values.volume.storageClass.storageClassName}}
  {{- end}}
