apiVersion: v1
kind: Service
metadata:
  name: jfb-{{ .Values.global.developer }}-image-svc
  namespace: {{ .Values.global.developer }}
spec:
  selector:
    app: jfb-{{ .Values.global.developer }}-image
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
