apiVersion: v1
kind: Service
metadata:
  name: jfb-{{ .Values.global.developer }}-deployment-svc
  namespace: {{ .Values.global.developer }}
spec:
  selector:
    app: jfb-{{ .Values.global.developer }}-deployment
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
