{{- if .Values.global.jfb.volume.src.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jf-src-dashboard-pvc
  namespace: {{ .Values.global.jfb.namespace  }}
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1G
  storageClassName: ""
---

apiVersion: v1
kind: PersistentVolume
metadata:
  name: jf-{{ .Values.global.jfb.namespace }}-src-dashboard-pv
spec:
  capacity:
    storage: 1G
  accessModes:
    - ReadWriteMany
  claimRef:
    namespace: {{ .Values.global.jfb.namespace }}
    name: jf-src-dashboard-pvc
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  {{- $srcDashboard := dict -}}
  {{- $_ := set $srcDashboard "src" (deepCopy .Values.global.jfb.volume.src) }}
  {{- $_ := set $srcDashboard.src "path" (printf "%s/%s" .Values.global.jfb.volume.src.path .Values.global.jfb.dir.dashboard) }}
  {{- include "lib.volume.type" $srcDashboard.src | nindent 2 }}
---
{{- end }}