{{- if .Values.global.jfb.volume.src.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jf-src-image-pvc
  namespace: {{ .Values.global.jfb.namespace  }}
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1G
  storageClassName: ""
---

apiVersion: v1
kind: PersistentVolume
metadata:
  name: jf-{{ .Values.global.jfb.namespace }}-src-image-pv
spec:
  capacity:
    storage: 1G
  accessModes:
    - ReadWriteMany
  claimRef:
    namespace: {{ .Values.global.jfb.namespace }}
    name: jf-src-image-pvc
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  {{- $srcImage := dict -}}
  {{- $_ := set $srcImage "src" (deepCopy .Values.global.jfb.volume.src) }}
  {{- $_ := set $srcImage.src "path" (printf "%s/%s" .Values.global.jfb.volume.src.path .Values.global.jfb.dir.image) }}
  {{- include "lib.volume.type" $srcImage.src | nindent 2 }}
---
{{- end }}