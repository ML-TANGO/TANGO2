import { ModalTemplateArgs } from '../../types';
export interface ModalRenderProps {
    modalList: ModalTemplateArgs[];
}
export default function ModalRender({ modalList }: ModalRenderProps): JSX.Element;
