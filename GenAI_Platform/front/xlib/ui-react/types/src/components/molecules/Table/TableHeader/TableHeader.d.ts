import { TableHeaderArgs } from '../types';
declare function TableHeader({ columns, isCheck, isAllChecked, InputSortIcon, headerStyle, checkHandler, sortHandler, }: TableHeaderArgs): JSX.Element;
export default TableHeader;
