import { PieChartArgs } from './types';
declare type Props = PieChartArgs;
declare function PieChart({ data, chartSize, chartFillColor, chartBolder, centerText, totalValue, }: Props): JSX.Element;
export default PieChart;
