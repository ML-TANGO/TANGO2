import { Properties as CSSProperties } from 'csstype';
interface EmptyboxArgs {
    isBox?: boolean;
    customStyle?: CSSProperties;
    text?: string;
    theme?: ThemeType;
}
export { EmptyboxArgs };
