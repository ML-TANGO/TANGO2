import ServingDeleteModalContent from '@src/components/modalContents/ServingDeleteModalContent';

function ServingDeleteModal({ type, data: modalData }) {
  return <ServingDeleteModalContent type={type} modalData={modalData} />;
}
export default ServingDeleteModal;
