import { InputNumber, Tooltip } from '@jonathan/ui-react';

// CSS Module
import classNames from 'classnames/bind';
import style from './RamBox.module.scss';
const cx = classNames.bind(style);

function RamBox() {
  return <div>RamBox</div>;
}

export default RamBox;
