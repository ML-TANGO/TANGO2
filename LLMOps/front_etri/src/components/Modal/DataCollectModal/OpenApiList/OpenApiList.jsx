import React from 'react';

// CSS Module
import classNames from 'classnames/bind';
import style from './OpenApiList.module.scss';

const cx = classNames.bind(style);

export default function OpenApiList() {
  return (
    <div className={cx('modal')}>
      <p>123</p>
    </div>
  );
}
