// Network

import { callApi, network, STATUS_SUCCESS } from '@src/network';

function UploadModalContainer({ data: modalData, type }) {
  const {
    deploymentWorkerId,
    systemLogData,
    systemLogLoading,
    traingId,
    projectName,
  } = modalData;

  return (
    <>
      
    </>
  );
}

export default UploadModalContainer;
