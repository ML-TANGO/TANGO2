import { PopupProps } from './type';
export default function Popup({ type, isLoading, isAnimation, frontTitle, popupTitle, popupContents, cancelBtnLabel, submitBtnLabel, handleCancel, handleSubmit, ...props }: PopupProps): JSX.Element;
