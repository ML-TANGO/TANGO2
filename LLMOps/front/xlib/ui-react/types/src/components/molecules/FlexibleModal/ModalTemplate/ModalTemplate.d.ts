import { ModalTemplateArgs } from '../types';
declare function FlexibleModal({ size, theme, control, content, modalKey, minimize, fullscreen, isMinimize, isMaximize, isFullScreen, onClickClose, onClickMinimize, onClickFullScreen, }: ModalTemplateArgs): JSX.Element;
export default FlexibleModal;
