declare type Props = {
    onClick?: () => void;
};
declare function SortIcon({ onClick }: Props): JSX.Element;
declare namespace SortIcon {
    var defaultProps: {
        onClick: undefined;
    };
}
export default SortIcon;
