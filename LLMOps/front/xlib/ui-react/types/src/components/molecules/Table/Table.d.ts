import { TableArgs } from './types';
declare function Table({ columns, data, isCheck, headerStyle, bodyStyle, isPageNation, pageRowCnt, PagingBtn, pagingBtnAlign, loading, InputSortIcon, getCheckedIndex, }: TableArgs): JSX.Element;
export default Table;
