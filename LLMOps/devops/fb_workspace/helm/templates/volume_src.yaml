{{- if .Values.global.jfb.volume.src.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jf-src-workspace-pvc
  namespace: {{ .Values.global.jfb.namespace  }}
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1G
  storageClassName: ""
---

apiVersion: v1
kind: PersistentVolume
metadata:
  name: jf-{{ .Values.global.jfb.namespace }}-src-workspace-pv
spec:
  capacity:
    storage: 1G
  accessModes:
    - ReadWriteMany
  claimRef:
    namespace: {{ .Values.global.jfb.namespace }}
    name: jf-src-workspace-pvc
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  {{- $srcWorkspace := dict -}}
  {{- $_ := set $srcWorkspace "src" (deepCopy .Values.global.jfb.volume.src) }}
  {{- $_ := set $srcWorkspace.src "path" (printf "%s/%s" .Values.global.jfb.volume.src.path .Values.global.jfb.dir.workspace) }}
  {{- include "lib.volume.type" $srcWorkspace.src | nindent 2 }}
---
{{- end }}