{{- if .Values.global.jfb.volume.src.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jf-src-ingress-management-pvc
  namespace: {{ .Values.global.jfb.namespace  }}
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1G
  storageClassName: ""
---

apiVersion: v1
kind: PersistentVolume
metadata:
  name: jf-{{ .Values.global.jfb.namespace }}-src-ingress-management-pv
spec:
  capacity:
    storage: 1G
  accessModes:
    - ReadWriteMany
  claimRef:
    namespace: {{ .Values.global.jfb.namespace }}
    name: jf-src-ingress-management-pvc
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  {{- $srcIngressManagement := dict -}}
  {{- $_ := set $srcIngressManagement "src" (deepCopy .Values.global.jfb.volume.src) }}
  {{- $_ := set $srcIngressManagement.src "path" (printf "%s/%s" .Values.global.jfb.volume.src.path .Values.global.jfb.dir.ingress_management) }}
  {{- include "lib.volume.type" $srcIngressManagement.src | nindent 2 }}
---
{{- end }}