apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.global.jfb.name.deployment }}
  namespace: {{ .Values.global.jfb.namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.global.jfb.name.deployment }}
  template:
    metadata:
      labels:
        app: {{ .Values.global.jfb.name.deployment }}
        {{- include "lib.label.jfb" . | nindent 8 }}
      annotations:
        "acryl.ai/appType": system
        checksum/config: {{ include "lib.lookup.settings" . }}
    spec:
      nodeSelector:
      {{- include "lib.nodeSelector.manage" . | nindent 8 }}
      serviceAccountName: jfb-deployment-sa
      initContainers:
      {{- include "lib.initContainer.db" . | nindent 6}}
      {{- if .Values.global.jfb.image.pullSecrets.enabled }}
      imagePullSecrets:
      - name: {{ .Values.global.jfb.image.pullSecrets.name }}
      {{- end }}
      containers:
      - name: {{ .Values.global.jfb.name.deployment }}
        image: {{ .Values.global.jfb.image.system.registry }}/{{ .Values.global.jfb.image.deployment }}
        imagePullPolicy: {{ .Values.global.jfb.image.pullPolicy }}
        command: [ "/bin/bash", "-c",]
        args: ["cd /app; ./run.sh; sleep infinity;"]
        resources:
          limits:
            cpu : 0.5
            memory : "512M"
          requests:
            cpu : 0.2
            memory : "256M"
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: jfb-settings
        {{- include "lib.probe.readiness" (dict "app" "deployment" )  | nindent 8 }}
        {{- include "lib.probe.liveness" (dict "app" "deployment" )  | nindent 8 }}
        volumeMounts:
        {{- include "lib.volume.volumeMounts" (dict "name" "jf-bin" )  | nindent 8 }}
        {{- include "lib.volume.volumeMounts" (dict "name" "jf-kube" )  | nindent 8 }}
        {{- include "lib.volume.volumeMounts" (dict "name" "jf-src" "enabled" .Values.global.jfb.volume.src.enabled )  | nindent 8 }}
        {{- include "lib.volume.volumeMounts.storage" . | nindent 8 }}
      volumes:
      {{- include "lib.volume.claim" (dict "name" "jf-bin")  | nindent 6 }}
      {{- include "lib.volume.claim" (dict "name" "jf-kube")  | nindent 6 }}
      {{- include "lib.volume.claim" (dict "name" "jf-src" "app" "deployment" "enabled" .Values.global.jfb.volume.src.enabled )  | nindent 6 }}
      {{- include "lib.volume.claim.storage" . | nindent 6  }}

